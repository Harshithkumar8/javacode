name: Java CI with Maven environment

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    

    steps:
      - name: Print variable
        run: echo "Deploying to ${{ vars.DEPLOY_ENV }}"
